<div>
  <div class="navbar bg-light mb-4 p-3 rounded">
    <h1>New list</h1>
  </div>
  <div class="container mt-4 lists-container new-list-form">
    <%= simple_form_for @list do |f| %>
      <%= f.input :name %>
      <%= f.button :submit %>
    <% end %>
    <%= link_to 'Back to lists', lists_path %>
  </div>
</div>
